# edu-insight 技术 TODO V1

更新日期：2026-06-16  
当前目标：把教师端和 Go 后端从可演示原型推进到可持续开发、可部署、可验收的真实系统。

## 1. 当前原则

- 当前活跃代码只维护在 `apps/edu-insight-teacher` 和 `apps/edu-insight-api`。
- feature 分支负责开发与构建校验。
- 合入 `dev` 后自动部署测试环境。
- 合入 `main/master` 后自动部署生产环境。
- 教师端 PC 优先，小程序后置。
- 先做基础数据闭环，再做分析、预警、同步。

## 2. 阶段 A：工程与部署基础

目标：保证每次开发都有稳定构建、镜像、部署链路。

TODO：
- 新增 `edu-insight` feature/PR 构建校验 workflow。
- 新增 `edu-insight` 教师端与 Go API 统一 Docker 构建推送 workflow。
- 新增测试环境部署 job。
- 新增生产环境部署 job。
- 更新 `infra/server/docker-compose.dev.yml`。
- 更新 `infra/server/docker-compose.yml`。
- 更新部署文档和端口表。
- 将教师端 API 地址改为环境可配置。

完成标准：
- feature 分支 PR 能跑构建校验。
- `dev` 分支对应路径变更能构建镜像并部署测试环境。
- 测试环境同时运行 teacher web 和 api。

## 3. 阶段 B：班级与师生后端化

目标：让成绩导入依赖真实学生、老师、班级、选科数据，而不是前端 mock。

TODO：
- 设计并实现组织基础接口：学校、年级、行政班。
- 设计并实现学生档案接口：列表、新增、编辑、导入校验。
- 设计并实现任课老师接口：列表、新增、编辑、导入校验、权限范围。
- 设计并实现学科与选科接口。
- 前端 `班级与师生` 页面切换为 API 驱动。
- 班级阶段作为后端固定属性返回，不允许在成绩导入中临时选择。

完成标准：
- 班主任可以维护一个真实班级。
- 学生、家长手机号、选科、任课老师信息可持久化。
- 成绩导入校验可以调用真实档案数据。

## 4. 阶段 C：考试与成绩完整闭环

目标：导入、校验、保存、修改、重算全部由后端主导。

TODO：
- 新增后端文件上传接口。
- 后端解析 CSV/XLS/XLSX。
- 后端根据班级阶段、学生选科、考试学科进行校验。
- 建立考试、导入批次、导入明细、单科成绩、聚合成绩表。
- 成绩修改后自动重算总分、排名、分层基础数据。
- 前端移除核心业务校验逻辑，只做展示与交互。

完成标准：
- 老师上传真实成绩文件后，系统返回校验结果。
- 校验通过后可提交入库。
- 成绩详情按学生行、学科列展示。
- 修改成绩后总分和排名同步更新。

## 5. 阶段 D：分析与预警

目标：让分析页面从样例展示转为真实计算。

TODO：
- 单次考试班级概览。
- 历次考试趋势。
- 学科维度分析。
- 学生个体分析。
- 分层统计。
- 学校模板预警规则。
- 班主任个性预警规则。
- 预警结果生成与处理。

完成标准：
- 班主任可基于真实历史考试看到趋势、分层和预警。
- 单科老师可查看单班和任课整体分析。

## 6. 阶段 E：同步中心与小程序

目标：在教师端可控发布成绩，家长和学生在小程序查看授权内容。

TODO：
- 展示策略配置。
- 成绩发布前预览。
- 家长/学生账号绑定。
- 通知任务创建。
- 小程序订阅消息。
- 发送记录与失败重试。
- 家长/学生小程序成绩详情。

完成标准：
- 班主任可选择一次考试并发布。
- 家长/学生只能看到被授权的数据粒度。
- 通知结果可追踪。

## 7. 最近执行顺序

1. 完成阶段 A 的 CI/CD 与部署配置。
2. 启动阶段 B，先实现班级与师生后端接口。
3. 将前端 `班级与师生` 页面接入 API。
4. 重构 `考试与成绩` 导入链路到后端校验。
5. 引入数据库持久化和迁移。
