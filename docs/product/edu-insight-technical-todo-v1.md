# edu-insight 技术 TODO V1

更新日期：2026-06-17  
当前目标：把教师端和 Go 后端从可演示原型推进到可持续开发、可部署、可验收的真实系统。

## 1. 当前原则

- 当前活跃代码只维护在 `apps/edu-insight-teacher` 和 `apps/edu-insight-api`。
- feature 分支负责开发与构建校验。
- 合入 `dev` 后自动部署测试环境。
- 合入 `main/master` 后自动部署生产环境。
- 教师端 PC 优先，小程序后置。
- 先做基础数据闭环，再做分析、预警、同步。

## 2. 阶段 A：工程与部署基础

目标：保证每次开发都有稳定构建、镜像、部署链路。

TODO：
- 新增 `edu-insight` feature/PR 构建校验 workflow。
- 新增 `edu-insight` 教师端与 Go API 统一 Docker 构建推送 workflow。
- 新增测试环境部署 job。
- 新增生产环境部署 job。
- 更新 `infra/server/docker-compose.dev.yml`。
- 更新 `infra/server/docker-compose.yml`。
- 更新部署文档和端口表。
- 将教师端 API 地址改为环境可配置。

完成标准：
- feature 分支 PR 能跑构建校验。
- `dev` 分支对应路径变更能构建镜像并部署测试环境。
- 测试环境同时运行 teacher web 和 api。

## 3. 阶段 B：班级与师生后端化

目标：让成绩导入依赖真实学生、老师、班级、选科数据，而不是前端 mock。

TODO：
- 已完成：当前班级工作区接口 `GET /api/classes/current`。
- 已完成：学生新增、编辑接口与 MySQL 持久化。
- 已完成：任课老师新增、编辑接口与 MySQL 持久化。
- 已完成：班级展示策略编辑接口与 MySQL 持久化。
- 已完成：前端 `班级与师生` 页面切换为 API 驱动。
- 已完成：班级阶段作为后端固定属性返回，不在成绩导入中临时选择。
- 待完成：学生档案批量导入真实文件上传与校验。
- 待完成：任课老师批量导入真实文件上传与校验。
- 待完成：多学校、多年级、多班级组织接口和权限边界。

完成标准：
- 班主任可以维护一个真实班级。
- 学生、家长手机号、选科、任课老师信息可持久化。
- 成绩导入校验可以调用真实档案数据。

## 4. 阶段 C：考试与成绩完整闭环

目标：导入、校验、保存、修改、重算全部由后端主导。

TODO：
- 已完成：考试列表、考试详情、导入保存、成绩修改 API。
- 已完成：考试与成绩 MySQL 持久化。
- 已完成：后端文件上传校验接口 `POST /api/exams/import/validate`。
- 已完成：后端解析 CSV/XLS/XLSX。
- 已完成：后端根据当前班学生档案、考试勾选学科校验基础字段、学科字段、学生匹配。
- 已完成：成绩详情支持动态学科列，考试选择哪些学科就展示哪些学科。
- 已完成：前端上传校验切到后端，前端不再解析 XLSX。
- 已完成：成绩修改后由后端基于动态学科成绩重算总分。
- 待完成：导入批次表、导入原始文件记录、导入明细版本记录。
- 待完成：单科成绩从 JSON 进一步拆成规范化明细表，便于排名、分析和筛选。
- 待完成：班级排名、学科排名、分层基础数据自动计算。
- 待完成：基于学生选科组合做缺考、错科、赋分/实际分双列校验。

完成标准：
- 已达到：老师上传真实成绩文件后，系统返回校验结果。
- 已达到：校验通过后可提交入库。
- 已达到：成绩详情按学生行、动态学科列展示。
- 已达到：修改成绩后总分同步更新。
- 未达到：排名、分层、分析基础数据同步更新。

## 5. 阶段 D：分析与预警

目标：让分析页面从样例展示转为真实计算。

TODO：
- 单次考试班级概览。
- 历次考试趋势。
- 学科维度分析。
- 学生个体分析。
- 分层统计。
- 学校模板预警规则。
- 班主任个性预警规则。
- 预警结果生成与处理。

完成标准：
- 班主任可基于真实历史考试看到趋势、分层和预警。
- 单科老师可查看单班和任课整体分析。

## 6. 阶段 E：同步中心与小程序

目标：在教师端可控发布成绩，家长和学生在小程序查看授权内容。

TODO：
- 展示策略配置。
- 成绩发布前预览。
- 家长/学生账号绑定。
- 通知任务创建。
- 小程序订阅消息。
- 发送记录与失败重试。
- 家长/学生小程序成绩详情。

完成标准：
- 班主任可选择一次考试并发布。
- 家长/学生只能看到被授权的数据粒度。
- 通知结果可追踪。

## 7. 最近执行顺序

1. 完成阶段 C 剩余的数据规范化：导入批次、单科成绩明细、排名快照。
2. 把 `分析与预警` 页面从静态样例改成后端真实计算。
3. 把 `同步中心` 页面从静态样例改成发布任务、策略检查和发送记录。
4. 为 `班级与师生` 补真实批量导入能力。
5. 扩展多校 SaaS、组织权限和角色切换后端模型。
